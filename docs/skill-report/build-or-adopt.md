# Recommendation: BUILD FROM SCRATCH

## BUILD FROM SCRATCH
*When: score <50/100, wrong language, too narrow scope, security failures*

→ Existing skill is a reference at best. It serves as a good cheat sheet for `infsh` commands, but it is not a true agentic skill capable of dynamically executing workflows.
→ Use gap analysis as requirements to rebuild.
→ Estimated effort: 6-8 hours
→ Key sections to include: 
   - **Interactive discovery phase:** (identify platform, audience, tone, format)
   - **Dynamic prompt generation and orchestration:** coordinate across text/image/video pipelines without fixed strings
   - **Robust error handling:** for LLM and generation APIs, addressing potential rate limits.
   - **Structured output formatting:** rather than dumping raw `.json`, structure it into a cohesive "campaign" folder for the user.
