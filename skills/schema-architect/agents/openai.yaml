interface:
  display_name: "Schema Architect"
  short_description: "Turns business requirements into a normalized schema and dialect-specific DDL."
  default_prompt: "Use $schema-architect to help with this task."

policy:
  allow_implicit_invocation: true
