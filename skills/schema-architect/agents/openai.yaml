interface:
  display_name: "Schema Architect"
  short_description: "Derives the database schema from business requirements. Selects optimal normalization levels (1NF->3NF->BCNF) and..."
  default_prompt: "Use $schema-architect to help with this task."

policy:
  allow_implicit_invocation: true
