interface:
  display_name: "API Mock Designer"
  short_description: "Designs realistic API mock servers. Goes beyond happy paths by designing stateful mocks (create order -> get order)..."
  default_prompt: "Use $api-mock-designer to help with this task."

policy:
  allow_implicit_invocation: true
