interface:
  display_name: "API Mock Designer"
  short_description: "Designs stateful API mocks and example payloads, including error and edge cases."
  default_prompt: "Use $api-mock-designer to help with this task."

policy:
  allow_implicit_invocation: true
