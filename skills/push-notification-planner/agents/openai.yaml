interface:
  display_name: "Push Notification Planner"
  short_description: "Plans APNs/FCM payloads, permission timing, tap routing, and analytics."
  default_prompt: "Use $push-notification-planner to help with this task."

policy:
  allow_implicit_invocation: true
