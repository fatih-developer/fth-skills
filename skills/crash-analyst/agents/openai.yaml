interface:
  display_name: "Crash Analyst"
  short_description: "Parse and analyze crash reports from tools like Crashlytics or Sentry. Interpret iOS symbolication and Android..."
  default_prompt: "Use $crash-analyst to help with this task."

policy:
  allow_implicit_invocation: true
