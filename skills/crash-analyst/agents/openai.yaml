interface:
  display_name: "Crash Analyst"
  short_description: "Symbolicates mobile crash reports, finds the root cause, and proposes a fix."
  default_prompt: "Use $crash-analyst to help with this task."

policy:
  allow_implicit_invocation: true
