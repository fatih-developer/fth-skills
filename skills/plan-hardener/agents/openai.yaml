interface:
  display_name: "Plan Hardener"
  short_description: "Assumption-first architecture review skill to stress-test project plans and expose hidden risks."
  default_prompt: "Use $plan-hardener to help with this task."

policy:
  allow_implicit_invocation: true
