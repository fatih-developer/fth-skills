interface:
  display_name: "Accessibility Enforcer"
  short_description: "Audits mobile UI for VoiceOver, TalkBack, and WCAG issues and proposes accessible code."
  default_prompt: "Use $accessibility-enforcer to help with this task."

policy:
  allow_implicit_invocation: true
