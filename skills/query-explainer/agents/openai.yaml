interface:
  display_name: "Query Explainer"
  short_description: "Explains EXPLAIN ANALYZE plans in plain language and points to the fix."
  default_prompt: "Use $query-explainer to help with this task."

policy:
  allow_implicit_invocation: true
