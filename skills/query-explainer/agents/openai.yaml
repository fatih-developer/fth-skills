interface:
  display_name: "Query Explainer"
  short_description: 'Execution plan''i (EXPLAIN ANALYZE) insanın anlayacağı dile çevirir. "Seq Scan" neden kötü, "Hash Join" ne zaman iyi...'
  default_prompt: "Use $query-explainer to help with this task."

policy:
  allow_implicit_invocation: true
