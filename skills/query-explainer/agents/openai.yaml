interface:
  display_name: "Query Explainer"
  short_description: "Translates execution plans (EXPLAIN ANALYZE) into human-readable language. Explains why Seq Scan is bad and when..."
  default_prompt: "Use $query-explainer to help with this task."

policy:
  allow_implicit_invocation: true
