interface:
  display_name: "Proje Analizcisi (Alias)"
  short_description: "Deprecated Turkish alias that routes to project-analyzer, which writes Turkish reports on request."
  default_prompt: "Use $proje-analizcisi to run project-analyzer on this project and write the reports in Turkish."

policy:
  allow_implicit_invocation: false
