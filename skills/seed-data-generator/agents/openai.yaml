interface:
  display_name: "Seed Data Generator"
  short_description: "Generates realistic test data preserving referential integrity. Reads schemas, foreign keys, and builds relationships."
  default_prompt: "Use $seed-data-generator to help with this task."

policy:
  allow_implicit_invocation: true
