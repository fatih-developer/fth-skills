interface:
  display_name: "Release Orchestrator"
  short_description: "Plans mobile versions, changelogs, beta tracks, and staged rollouts."
  default_prompt: "Use $release-orchestrator to help with this task."

policy:
  allow_implicit_invocation: true
