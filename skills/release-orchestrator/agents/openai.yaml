interface:
  display_name: "Release Orchestrator"
  short_description: "Manage the mobile release lifecycle. Produce version bumps, changelog generation, TestFlight/Internal Track..."
  default_prompt: "Use $release-orchestrator to help with this task."

policy:
  allow_implicit_invocation: true
