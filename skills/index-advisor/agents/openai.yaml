interface:
  display_name: "Index Advisor"
  short_description: "Analyzes existing queries and schema to detect missing indexes. Suggests covering and composite indexes to optimize..."
  default_prompt: "Use $index-advisor to help with this task."

policy:
  allow_implicit_invocation: true
