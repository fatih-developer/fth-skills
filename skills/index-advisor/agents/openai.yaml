interface:
  display_name: "Index Advisor"
  short_description: "Recommends missing, composite, covering, and removable indexes for slow queries."
  default_prompt: "Use $index-advisor to help with this task."

policy:
  allow_implicit_invocation: true
