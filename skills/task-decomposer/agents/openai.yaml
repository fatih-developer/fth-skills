interface:
  display_name: "Task Decomposer"
  short_description: "Breaks large tasks into subtasks with dependencies, order, and success criteria."
  default_prompt: "Use $task-decomposer to help with this task."

policy:
  allow_implicit_invocation: true
