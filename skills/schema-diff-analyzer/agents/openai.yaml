interface:
  display_name: "Schema Diff Analyzer"
  short_description: "Finds schema drift between environments and plans a risk-ordered sync."
  default_prompt: "Use $schema-diff-analyzer to help with this task."

policy:
  allow_implicit_invocation: true
