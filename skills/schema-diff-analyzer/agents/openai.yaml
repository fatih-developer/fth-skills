interface:
  display_name: "Schema Diff Analyzer"
  short_description: "Detects schema differences between environments (dev vs staging vs prod). Generates actionable migration paths..."
  default_prompt: "Use $schema-diff-analyzer to help with this task."

policy:
  allow_implicit_invocation: true
