interface:
  display_name: "Data Lineage Tracer"
  short_description: "Traces upstream and downstream lineage of tables and columns and their blast radius."
  default_prompt: "Use $data-lineage-tracer to help with this task."

policy:
  allow_implicit_invocation: true
