interface:
  display_name: "Migration Strategist"
  short_description: "Manages major schema changes with zero-downtime using the expand-contract pattern, rather than writing unstable..."
  default_prompt: "Use $migration-strategist to help with this task."

policy:
  allow_implicit_invocation: true
