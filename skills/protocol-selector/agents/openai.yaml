interface:
  display_name: "Protocol Selector"
  short_description: "Chooses REST, GraphQL, gRPC, or event-driven APIs with a trade-off table."
  default_prompt: "Use $protocol-selector to help with this task."

policy:
  allow_implicit_invocation: true
