interface:
  display_name: "Protocol Selector"
  short_description: "Selects the most suitable API protocol (REST, GraphQL, gRPC) based on project requirements like latency, payload..."
  default_prompt: "Use $protocol-selector to help with this task."

policy:
  allow_implicit_invocation: true
