interface:
  display_name: "Breaking Change Detector"
  short_description: "Compares two OpenAPI/API specification versions (V1 vs V2) to detect breaking changes and backward compatibility issues."
  default_prompt: "Use $breaking-change-detector to help with this task."

policy:
  allow_implicit_invocation: true
