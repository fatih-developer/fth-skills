interface:
  display_name: "Session Transfer"
  short_description: "Create and resume from session handoff documents for seamless AI agent session transfers. Preserves complete context across sessions to solve context exhaustion."
  default_prompt: "Use $session-transfer to save current state, create a handoff document, or resume from an existing handoff when context is getting full."

policy:
  allow_implicit_invocation: true
