interface:
  display_name: "Offline Sync Designer"
  short_description: "Designs offline storage, optimistic UI, conflict resolution, and background sync."
  default_prompt: "Use $offline-sync-designer to help with this task."

policy:
  allow_implicit_invocation: true
