interface:
  display_name: "Contract-First Designer"
  short_description: "Writes OpenAPI/AsyncAPI specifications before writing any code. Determines provider-consumer contracts and endpoint..."
  default_prompt: "Use $contract-first-designer to help with this task."

policy:
  allow_implicit_invocation: true
