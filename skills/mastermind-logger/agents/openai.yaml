interface:
  display_name: "Mastermind Logger"
  short_description: "Keeps a cross-referenced project knowledge wiki updated with durable learnings."
  default_prompt: "Use $mastermind-logger to save our current session insights."

policy:
  allow_implicit_invocation: false
