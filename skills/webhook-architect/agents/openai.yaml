interface:
  display_name: "Webhook Architect"
  short_description: "Designs signed, retried, idempotent outbound webhooks with a dead-letter queue."
  default_prompt: "Use $webhook-architect to help with this task."

policy:
  allow_implicit_invocation: true
