interface:
  display_name: "Webhook Architect"
  short_description: "Webhook altyapısının gönderici (provider) tarafını tasarlar. Sadece veri yollamakla kalmaz; event şema tasarımı,..."
  default_prompt: "Use $webhook-architect to help with this task."

policy:
  allow_implicit_invocation: true
