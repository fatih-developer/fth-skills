interface:
  display_name: "Onboarding Designer"
  short_description: "Designs mobile first-run flows, permission sequencing, and early retention hooks."
  default_prompt: "Use $onboarding-designer to help with this task."

policy:
  allow_implicit_invocation: true
