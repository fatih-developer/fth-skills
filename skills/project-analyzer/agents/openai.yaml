interface:
  display_name: "Project Analyzer"
  short_description: "Evidence-based codebase analysis: overview, architecture, code quality, risks, and API endpoint reports."
  default_prompt: "Use $project-analyzer to scan this project, analyze architecture, code quality, security, and performance, and write the reports to docs/analyze/ in my language."

policy:
  allow_implicit_invocation: true
