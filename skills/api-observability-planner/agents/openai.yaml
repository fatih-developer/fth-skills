interface:
  display_name: "API Observability Planner"
  short_description: "Architects which metrics to collect, how logs should be formatted, and how distributed tracing should be implemented..."
  default_prompt: "Use $api-observability-planner to help with this task."

policy:
  allow_implicit_invocation: true
