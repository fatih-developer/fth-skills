interface:
  display_name: "Skill Security (Alias)"
  short_description: "Deprecated alias for security-auditor: skill audits, trust scores, and runtime guard rules."
  default_prompt: "Use $skill-security to audit this skill with security-auditor and report the trust score and findings."

policy:
  allow_implicit_invocation: false
