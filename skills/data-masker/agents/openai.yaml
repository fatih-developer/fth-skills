interface:
  display_name: "Data Masker"
  short_description: "Masks production data for test and development environments (Data Masking). Detects PII (email, SSN, credit cards)..."
  default_prompt: "Use $data-masker to help with this task."

policy:
  allow_implicit_invocation: true
