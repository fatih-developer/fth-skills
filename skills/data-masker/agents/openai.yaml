interface:
  display_name: "Data Masker"
  short_description: "Detects PII and masks production data for safe test and staging copies."
  default_prompt: "Use $data-masker to help with this task."

policy:
  allow_implicit_invocation: true
