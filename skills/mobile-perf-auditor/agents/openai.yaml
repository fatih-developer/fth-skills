interface:
  display_name: "Mobile Perf Auditor"
  short_description: "Diagnoses mobile cold start, jank, memory, battery, and bundle-size problems."
  default_prompt: "Use $mobile-perf-auditor to help with this task."

policy:
  allow_implicit_invocation: true
