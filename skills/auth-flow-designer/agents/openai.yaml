interface:
  display_name: "Auth Flow Designer"
  short_description: "Chooses API keys, JWT, OAuth2, or mTLS and designs token lifecycles and scopes."
  default_prompt: "Use $auth-flow-designer to help with this task."

policy:
  allow_implicit_invocation: true
