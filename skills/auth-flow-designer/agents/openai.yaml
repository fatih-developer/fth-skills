interface:
  display_name: "Auth Flow Designer"
  short_description: "Determines whether to use API keys, JWT, OAuth2, or mTLS. Designs token lifespans, refresh token strategies, and..."
  default_prompt: "Use $auth-flow-designer to help with this task."

policy:
  allow_implicit_invocation: true
