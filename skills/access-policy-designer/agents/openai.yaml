interface:
  display_name: "Access Policy Designer"
  short_description: "Designs and implements row-level security (RLS), column-level masking, and role-based access control policies..."
  default_prompt: "Use $access-policy-designer to help with this task."

policy:
  allow_implicit_invocation: true
