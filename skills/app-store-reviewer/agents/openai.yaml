interface:
  display_name: "App Store Reviewer"
  short_description: "Checks apps against App Store and Play Store guidelines and fixes store metadata."
  default_prompt: "Use $app-store-reviewer to help with this task."

policy:
  allow_implicit_invocation: true
